# Context Index

This directory holds the accumulated factual record of the simulation repository, the documents that establish the physical properties of the robots, the derivations behind their actuator configuration, and the mathematics of the learning architectures implemented here. A context document is descriptive rather than prescriptive, it records findings verified against the assets, the configuration, and the source, and it carries file and symbol citations so that a later reader need not repeat the derivation. Documents that propose changes rather than record facts belong in [../plans](../plans/README.md) instead.

The scope of this directory is deliberately narrow. It holds only what is specific to this repository, the robots it defines and the algorithms it implements. The wider investigation record of the workspace, covering the co-optimisation pipeline, the Isaac Lab and rsl_rl internals, the experiment history, and the literature survey, lives one level up in `../../context/` and is indexed by `../../context/README.md`. A reader seeking why a training run behaved as it did should look there, a reader seeking how this robot is parameterised should look here.

## Reading protocol

Read only the documents the register names as bearing on the matter at hand. They are largely independent of one another, with three exceptions. [MorAL.md](MorAL.md) refers the reader to [HIM.md](HIM.md) for the contrastive estimator it declines to re-derive. [joint_control_analysis_quadruped.md](joint_control_analysis_quadruped.md) refers the reader to [joint_control_analysis.md](joint_control_analysis.md) for the whole of the second order theory it applies, and states only what that document lacks. And [quadruped.md](quadruped.md) refers to both of the other two quadruped documents for every derivation it reports the result of.

Before altering any actuator gain, consult [BRS.md](BRS.md) rather than reasoning from the configuration alone, since the configuration records the chosen values but not the inertias and bandwidth constraints that justify them, and [KScale.md](KScale.md) for the second biped, whose document adds the two treatments [BRS.md](BRS.md) omits, being the armature's place in the mass matrix and the distinction between the swing inertia a joint accelerates in the air and the stance load it must hold on the ground. That second distinction is not academic, a derivation that observed only the first having produced a KScale that could not stand, and section 16 of that document records the training run it cost. Before altering the hybrid internal model estimator, consult [HIM.md](HIM.md), which is the only account of the contrastive machinery its implementation relies upon. Before reading a line of the vendored `MorAL/` tree, consult [MorAL.md](MorAL.md), which records its architecture in full together with the eleven defects a reader would otherwise mistake for design.

## Document register

| Document | Subject | Last revised | Currency |
|---|---|---|---|
| [BRS.md](BRS.md) | SD_BRS1 actuator gain analysis, inertias, natural frequencies, damping ratios, recommended gains | 2026-07-29 | Current, its damping recommendations were adopted only later, see the note below |
| [KScale.md](KScale.md) | The KScale biped's physical parameterisation, its link mass and length inventory, joint topology, root frame and sole geometry, effective inertias in both the swing and the stance regime, stance load analysis, the actuator class identification and the effort and velocity limits, the calculated and the identified actuator gain sets with a comparison of the two, ideal gain derivation including the discrete stability bound of an explicitly integrated servo, symmetry mirror, collision audit and the diagnosis of the 2026-08-24 training failure | 2026-09-11 | Current, restructured 2026-08-25 to the analytical order of BRS.md, its gain derivation superseding the swing-only one that preceded it, and extended 2026-09-04 with sections 7 to 9 and 10.5 recording the identified actuator gains that replaced the calculated set on 2026-09-03, their stance load and equilibrium analysis and a quantitative comparison of the two sets, and revised the same day with the stance regime treated properly throughout, section 4.9 taking the stance reflected inertia as the proximal assembly's inertia about the joint axis rather than as the vertical lift alone and section 10.6 adding the single support hold torque and the gravitational stiffness, from which section 11 derives the calculated set as hip pitch 200 and 20, hip roll 250 and 28, hip yaw 100 and 11, knee 300 and 10 and both ankles 120 and 1.8, with section 11.10 recording that four joints of six stand at their effort limit for about half of every episode and section 11.2 recomputed at the action scale of 0.25 the configuration now carries. Sections 7 to 20 carry the numbering they took in that pass, the former sections 7 to 17 having each moved by three. The saturation check of section 11.2 is computed at the configured action scale, which became 0.25 on 2026-09-04, and section 20 records that the check and the configuration disagreed between 2026-08-25 and that date, every run in the series having trained at 0.4. Extended 2026-09-04 with a third dated addition recording the run `kscale_flat/2026-09-04_10-40-30`, which was asked to walk on swing derived distal gains and never stood across thirty thousand iterations, confirming the gravitational stiffness floor of section 10.6 experimentally, and which records three general results, that a torque logged at a position controlled joint is the policy driving it rather than the world disturbing it and may not be divided by a deflection to give a stiffness, that a gain set whose joints share one function is not separable, and that the action term carries `clip` unset so the action scale bounds nothing the policy commands, the reduction to 0.25 having RAISED the ankle roll stop residency. Extended 2026-09-08 with sections 5.1, 5.2 and 11.1.1 and a fourth dated addition, following the run `kscale_flat/2026-09-07_11-08-18` which carried those stiffnesses in full and never stood. Section 5.1 identifies the actuator classes from the two digit joint name suffixes against the K-Scale Labs metadata recorded at [/ws/context/kscale-opensource.md](../../context/kscale-opensource.md), establishing the two `_04` joints as robstride_04 and therefore under provisioned at the robstride_03 peak of 60 Nm, and section 5.2 records the doubling of every effort and velocity limit to 120 Nm and 40 rad/s at the proximal joints and 34 Nm and 20 rad/s at the ankles, which admits the ankle roll single support hold of 25.07 Nm that 17 Nm forbade. Section 11.1.1 adds the criterion the earlier derivation lacked, an explicitly integrated damper being conditionally stable at `D dt / I_swing` below two against the SWING inertia, which the earlier stance sized damping violated at the hip yaw and the ankle roll and which the identified set of section 7 violates at three joints in every environment, and the calculated set is accordingly revised to hip pitch 200 and 21.5, hip roll 250 and 28, hip yaw 100 and 3.3, knee 300 and 12 and the ankles 120 and 1.3 and 120 and 1.0, applied to `assets/config/kscale_identified_cfg.py` on 2026-09-08. Section 11.10 additionally records that the `DCMotor` torque speed curve left the ankles 0.69 Nm of available torque at their measured ninety fifth percentile speed under the original 10 rad/s velocity limit, a factor of 25 recovered by the doubling. Corrected 2026-09-09 following the runs `2026-09-08_06-50-00` and `2026-09-08_07-39-29`, which carried those gains and limits and both collapsed to seventeen steps of episode length by iteration 400. The correction establishes that `velocity_limit` shapes the torque speed curve only and that the speed the solver enforces is `velocity_limit_sim`, which `assets/config/kscale_identified_cfg.py` never sets and which therefore resolves to the placeholder `velocity="10"` carried by every joint of `kscale.urdf`, so the doubling of section 5.2 raised no joint's speed and instead tripled the torque the actuator delivers while pressed against an unmoved clamp, from 30 Nm to 90 Nm at the proximal joints and from zero to 17 Nm at the ankles. The diagnosis of section 11.10 is thereby reversed, the ankle having been correctly brought to rest by a curve matched to the clamp rather than starved by a mis set parameter. The correction also exonerates the action scale, the two runs bracketing it at 0.25 and 0.4 and failing identically, and the separation floor, which contributes one thousandth of the deficit, and it records the corroboration that the TRON1 configuration sets `velocity_limit_sim` explicitly on every actuator where the KScale sets it nowhere. Extended 2026-09-09 with a fifth dated addition recording that the effort and velocity ceilings of `assets/config/kscale_identified_cfg.py` were replaced the same day by the published RobStride class figures of [/ws/context/kscale-opensource.md](../../context/kscale-opensource.md), `saturation_effort` taking the peak torque and `effort_limit` the derated soft limit, giving 60 and 42 Nm at the two upper hip joints, 120 and 84 at the hip pitch and knee and 17 and 11.9 at the ankles, with both velocity fields set to one number, the lesser of the declared and the firmware ceiling, at 18.849, 15.0 and 37.699 rad/s respectively, so that the solver clamp and the curve knee coincide by construction. That addition leaves two matters standing, the armature disagreeing with the class figures by a factor of two at the hips and three at the knee, and the ankle ceiling having fallen 65 per cent from the 34 Nm every earlier run carried. Corrected 2026-09-10 by a sixth dated entry recording that the K-Bot ceilings were withdrawn without ever being trained and the tree returned in full to `kscale_flat/2026-08-28_04-50-51`, the only run in this robot's history to have produced a serviceable gait, verified field by field against that run's `params/env.yaml` over sixty actuator fields and one hundred and three reward fields, with the solver iteration counts returned from four to the two the dump records and exactly one departure carried forward, `velocity_limit_sim` set equal to `velocity_limit` at 20.0 rad/s proximally and 10.0 at the ankles, which raises the proximal solver clamp from the URDF placeholder of 10.0 to meet the curve knee and changes nothing at the ankles. That entry also records that the reference run was launched from an uncommitted working tree, the closest committed state `cf82fc4` disagreeing with the dump at the action scale, at `keep_balance` and at `pen_feet_heading`, so the dump is the only authority for what ran. Extended 2026-09-11 with a seventh dated addition settling the shank and foot bounding box overlap that section 18 left open, which is a true convex hull intersection of 11.05 mm and not a mesh intersection, the raw meshes clearing by 4.90 mm, the importer's `convex_hull` default having filled the ankle slot in the shank fork. That entry supersedes section 18's closing judgement that self collision is off for this robot, records the two pairs excluded through `UsdPhysics.FilteredPairsAPI` at spawn time and the two routes rejected before it, being `convex_decomposition` which exhausts GPU memory and the `prestartup` event mode which `event_manager.py:363` refuses under `replicate_physics`, and leaves standing the torso against `kd_d_201r_6061` at 1.59 mm as untested against its raw meshes Extended 2026-09-16 with an eighth dated addition recording that the nominal pose was replaced on that date by the published K-Scale K-Bot standing posture of hip pitch 0.34907, knee 0.87266 and ankle pitch minus 0.52360, which closes the sagittal chain as its predecessor did and gives a standing height of 0.73540 m, and warning that this figure must not be read from the knee sweep of section 16.3, which holds the hip pitch at 0.1 and would give 0.712. That entry records the three parameters carrying the standing height and their new values, that the deeper fold LOWERS the hip pitch single support load 17 per cent while raising the knee's 69 per cent and the hip yaw hold by a factor of 3.4, and one finding that holds at every pose and every limit set this robot has carried, that the gravitational stiffness of both ankles in single support is positive and larger than their configured stiffnesses of 50 and 20, so both ankles are statically unstable in single support and have been in every run of its history |
| [HIM.md](HIM.md) | The hybrid internal model architecture, its estimator, and its contrastive objective | 2026-05-15 | Current |
| [joint_control_analysis.md](joint_control_analysis.md) | PD joint control as a second order system, action scaling, velocity saturation | 2026-03-18 | Current, the analysis is analytical rather than configuration bound |
| [MorAL.md](MorAL.md) | The vendored `MorAL/` clone, its software architecture, morphology mechanism, policy and training architecture, rewards, curricula and defects | 2026-08-10 | Current, read against commit `0ac74da8` of that tree |
| [quadruped.md](quadruped.md) | The quadruped's environment configuration as built, its rewards with the survey that settled them, its events, terrain, class hierarchy and task registry | 2026-08-19 | Current, written on completion of the quadruped integration |
| [quadruped_xml_to_urdf_conversion.md](quadruped_xml_to_urdf_conversion.md) | The MuJoCo to URDF conversion of the quadruped, and the general procedure for any such conversion in this project | 2026-08-19 | Current |
| [joint_control_analysis_quadruped.md](joint_control_analysis_quadruped.md) | The quadruped's gains, velocity ranges and action scale, with the theory of choosing a stiffness when none is given | 2026-08-19 | Current |

## Document summaries

### BRS.md

The physics grounded derivation of the stiffness and damping gains for the SD_BRS1 biped, and the authoritative source for that robot's inertial properties. It extracts the link mass inventory, the leg geometry, and the joint topology from the assembly URDF, develops the proportional derivative actuator model as a second order system with its bandwidth constraint at the control rate, and then computes the effective inertia seen by each joint through the parallel axis theorem, obtaining 6.494 for hip pitch, 6.499 for hip roll, 1.272 for the knee, 0.039 for ankle pitch, and 0.033 for ankle roll, in kilogram metres squared. From these it evaluates the natural frequency and damping ratio of the configured gains joint by joint, adds a gravitational torque analysis, compares the result against the co-resident TRON1 configuration, and closes with a recommended gain range per joint together with a saturation check against the configured effort limits.

Its practical consequence is recorded elsewhere and belongs on this index. The stiffness half of its recommendations was adopted into the configuration while the damping half was not, which left the proximal joints at damping ratios between 0.07 and 0.16 against the 0.7 the document targets, and that discrepancy was later identified as a root cause of the degraded SD_BRS1 gait. The diagnosis is in `../../context/brs_gait.md` and the remedy in `../../plans/GAIT_STRATEGY.md`, whose Phase 0 finally applied the damping figures this document derived.

### KScale.md

The physical parameterisation of the KScale biped, the second sole footed biped in the tree, and the authoritative source for that robot's geometry, inertias and actuator gains in the manner [BRS.md](BRS.md) is for the SD_BRS1. It was restructured on 2026-08-25 to follow that document's analytical order, so that the same calculations are performed and validated for every robot in the workspace, and it now runs from the URDF extraction of section 2 through the effective inertia calculation of section 4, the stance load analysis of section 7 and the ideal gain derivation of section 8 to the recommended values of section 9, with the KScale specific material on the root frame, the sole, the pose, the mirror and the collision audit following in sections 11 to 15.

Two analytical additions distinguish it from [BRS.md](BRS.md) and both were forced by this robot rather than chosen. The armature is carried into the effective inertia throughout, since Isaac Lab writes it onto the PhysX joint and at the KScale ankles it exceeds the link inertia by factors of 1.92 and 6.75, where on the SD_BRS1 it is a negligible correction. And the document separates the swing inertia, being the distal subtree about the joint axis, from the stance load, being the torque the joint must hold against the ground reaction force, which are answered by different equations and disagree by up to a factor of 3.68 here.

That separation is the document's central lesson and it is recorded because ignoring it cost a full training run. The gains derived on 2026-08-24 placed every natural frequency inside a bandwidth band computed against the swing inertia alone, and never asked whether a joint could hold the robot's weight. The knee at 25 Nm/rad against a static double support load of 8.109 Nm gave the robot a settled equilibrium at a base height of 0.4362 m against a nominal 0.77161 m, so it collapsed the moment it was set down. Section 16 records the diagnosis from the training logs, where the episode length sat at 40 steps for 18000 iterations with every termination attributed to `low_height` and none to `base_contact`, which is the signature of legs folding rather than a body toppling.

Its practical consequence is that the KScale reward set matches the SD_BRS1 term for term, twenty seven terms with identical functions and weights, every differing parameter being a derived robot specific quantity rather than an inherited one, and that the actuator gains are now derived against both regimes with the stance sag and the settled base height quoted for each. The changes it justifies are recorded in [../plans/kscale_integration.md](../plans/kscale_integration.md).

Consult section 13 before parameterising anything against the KScale standing height, since that section establishes the current nominal pose, shows it to be the first that closes the sagittal chain and so the first to rest the sole flat, records by a swept table that a knee of 0.4 rad lowers this robot by three millimetres rather than by the amount a reader would expect, and fixes the spawn height at the standing height plus the 0.02 m settling margin the SD_BRS1 already uses.


### HIM.md

The mathematical account of the hybrid internal model, the learned internal state estimator that the `himloco` package implements. It explains how the architecture combines supervised velocity estimation with prototype based contrastive representation learning to compress the proprioceptive history into a compact latent, which together with a predicted base velocity augments the actor observation at every step, so that the policy may infer unobservable quantities such as terrain contact, slip, and external disturbance from sensor history alone and therefore require no privileged teacher observation at deployment. Its most useful passages are the step by step walkthrough of the Sinkhorn normalisation used to form the prototype assignment, annotated with the numerical stability measures that motivate each step, and the correlation of that derivation against the exact lines of `himloco/himloco/modules/him_estimator.py` which implements it. An appendix closes the document.

Note that this file sat at `himloco/HIM.md` until 2026-07-30 and is referenced from a comment in the estimator source, which was updated to the new path in the same pass.

### joint_control_analysis.md

An analytical treatment of the proportional derivative joint controller as a mass spring damper system, and of the way action scaling interacts with actuator velocity limits to destabilise training. It derives the equation of motion, the characteristic equation, the natural frequency, and the damping ratio, describes the response in each damping regime, and then applies the results numerically to the SoleFoot robot. Its central contribution is the causal chain from an excessive action scale through velocity saturation to sustained oscillation and training divergence, together with a stability boundary and concrete recommendations.

This is the sole copy. A byte identical duplicate stood at `../../context/joint_control_analysis.md` in the workspace record until 2026-07-30, when it was deleted and this copy made canonical, on the ground that the subject is the actuator parameterisation of a robot defined in this repository and therefore belongs beside the SD_BRS1 gain derivation in [BRS.md](BRS.md). The workspace index records the move, and the hub `../../context/knowledge_base.md` now cites this path.

Its section 6.5 carries one defect, recorded rather than repaired. The peak transient velocity is bounded there by `A omega_n / sqrt(1 - zeta^2)`, which overestimates by a factor near 2.5 because it ignores the exponential decay before the sine reaches its first peak. The correction is derived in [joint_control_analysis_quadruped.md](joint_control_analysis_quadruped.md) section 4.2.

### quadruped.md

The quadruped's environment configuration as built, in the manner this index's [tron1.md](tron1.md) records the TRON1's physical parameterisation. It states the robot's mass, stance and topology, its three actuator groups with their derived gains, and then the seventeen reward terms together with the survey of published quadruped configurations that settled each weight, which is recorded nowhere else. It explains why the penalty rebalancing against the canonical weights is deliberately partial, this framework lacking the reward clipping every reference relies upon, why a foot clearance reward is omitted despite the survey recommending one, and why the air time threshold must not be zero. It closes with the event and terrain configuration, the class hierarchy and the twelve registered task identifiers, four of which are registered but must not yet be launched.

### quadruped_xml_to_urdf_conversion.md

The conversion of `my_design.xml` into the Isaac Lab asset, and the general procedure for any future MuJoCo to URDF conversion in this project. It states the conventions of both formats before using them, the half extent rule for boxes and the radius with half length rule for cylinders, the absence of a capsule primitive from URDF, the fact that MuJoCo body positions and URDF joint origins express the same quantity and therefore transcribe directly, and the rule that a spherical or capsular foot places its frame at the centre so that the standing height exceeds the kinematic drop by a radius. It then records the inertia audit that established the source model's implied density of 700 kilogrammes per cubic metre and the four corrections that audit forced, the calf's mass and inertia, the two capsule geometries, the mirrored abduction axes and the buried foot. It closes with the clearance audit, the verification against the compiled MuJoCo model, and a ten point checklist for the next conversion.

### joint_control_analysis_quadruped.md

The derivation of the quadruped's gains, velocity ranges and action scale. It deliberately restates none of the second order theory, referring the reader to [joint_control_analysis.md](joint_control_analysis.md) for all of it, and contributes the two treatments that document lacks because the biped's gains arrived already identified from hardware. The first is how a proportional gain is chosen when none is given, which turns on the observation that a proportional law cannot hold a load without an error, so the stance torque and a tolerated sag impose a floor, and that sizing against response rather than load would give the most heavily loaded joint the softest gain. This explains why every published configuration carries nearly uniform stiffness across joints of very different inertia. The second is how a joint velocity range is chosen, which turns on the fact that Isaac Lab's velocity limit is a no load speed rather than a clamp, so the quantity that bounds useful motion is the corner speed where the torque ramp meets the effort ceiling.

Two corrections to the plan that specified this work are recorded in it. The abduction effective inertia at the crouch is 0.023674 and not the 0.024292 obtained by hand, and more consequentially the stance torques were measured by settling the robot rather than computed from a lever arm, which moved the abduction load from a predicted 4.046 newton metres to a measured 0.129 and the hip's from 0.226 to 2.256. No gain changed, every chosen value clearing its true floor with more margin than the plan claimed, but the binding joint is the knee and not the abduction joint.

### MorAL.md

The complete reading of the `MorAL/` tree vendored at this repository's root, written so that a later session may judge what it offers the co-optimisation work without re-reading seven thousand lines of Isaac Gym era code. It establishes first what the clone is, namely a StochLab reimplementation built on the HIMLoco codebase rather than the authors' release, whose commit history records the morphology network being introduced, removed and restored, and whose GenLoco flag names the procedural morphology randomisation it implements. It then traces the simulation substrate, the control loop timing and the widened seven value step signature that exists so that the estimator receives an uncontaminated next state at an episode boundary.

Its central section is the morphology mechanism. A standalone script performs parametric surgery on one donor Go1 URDF to write 3600 variants and their parameter files, sampling one overall size scale, deriving limb scales allometrically, and recovering masses from volumes through densities clipped into physical bands, a population measured here at 4.96 to 79.99 kilogrammes. Each environment then loads its own asset at construction and keeps it for the life of the run, with two derived per environment quantities that matter well beyond this clone, a nominal base height computed from the leg link lengths and an actuator gain scale computed from the total mass. The decisive negative result is that nothing reloads a morphology and no design optimiser exists anywhere in the tree, so MorAL is a generalist controller over a fixed design distribution rather than a co-design method.

The remaining sections give the exact index layout of the 264 dimensional privileged observation, the five networks of the policy with their input compositions, the three phase training architecture and the loss composition each phase selects, the effective reward weights after the GenLoco overrides, the terrain and command curricula, the domain randomisation, and the persistence and export paths. It closes with a register of eleven defects established by reading, of which three are consequential, the degree of freedom properties of the first generated asset being applied to every environment so that the mass scaled effort limits are discarded and an eighty kilogramme robot is clipped at a thirteen kilogramme robot's ceiling, the foot clearance setpoint carrying the wrong sign for the frame it is evaluated in, and the TorchScript exporter being unable to produce a runnable policy. A closing assessment separates the three architectural ideas that transfer to the co-optimisation pipeline, the supervised design regression head, the morphology dependent reward setpoint and the residual phase structure, from the code that carries them, which does not.

## Conventions

Codebase citations take the form `path:line`, resolved against this repository's root. Line numbers were correct when written and drift as the sources change, so a citation should be treated as a pointer to a symbol rather than to a position, and the symbol located by name when the line no longer matches.

Cross references within this directory use the bare filename. References to the workspace level record take the form `../../context/NAME.md` and to the workspace plans `../../plans/NAME.md`. References to this repository's own architecture document take the form `../ARCHITECTURE.md`.

The writing and context conventions governing these documents are those of the workspace, recorded in `../../CLAUDE.md`.

## Related documentation

[../ARCHITECTURE.md](../ARCHITECTURE.md) describes the Isaac Lab and rsl-rl task integration of this repository, its directory roles, class hierarchies, and the steps for adding a task. [../README.md](../README.md) covers installation and standalone usage. [../GEMINI.md](../GEMINI.md) is a high level companion overview generated for tooling. All three remain at the repository root because they describe the repository as a whole rather than a single investigation.

A document formerly lived beside the asset it described, `base_robot.md` in the TRON1 URDF directory, recording the link and joint structure of `base_robot.urdf`. It did not survive the move of the extension package to `environments/environments/` and is no longer retained. The observation it carried survives in two places, the URDF itself at `../environments/environments/assets/urdf/solefoot/tron1/base_robot.urdf`, which remains the authority on the topology, and [tron1.md](tron1.md), which records the moment of inertia derivation for every link of that file.
